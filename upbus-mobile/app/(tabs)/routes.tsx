import { View, Text } from 'react-native';

export default function RoutesScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0f0f1a', justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff' }}>เส้นทาง</Text>
    </View>
  );
}
